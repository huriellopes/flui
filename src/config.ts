// URL base da API. Troque aqui caso o subdomínio mude.
// A API sobe no Contabo atrás do Nginx Proxy Manager (SSL Let's Encrypt).
// Endpoint via sslip.io (proxy TLS apontando para o IP do servidor).
export const API_URL = 'https://flui-api.217.76.60.113.sslip.io/api';
